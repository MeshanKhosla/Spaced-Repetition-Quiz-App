import React from 'react'

// Not being used currently
const ResultsPage = (props) => {
    return (
        <div>
            <h1>{props.results}</h1>
        </div>
    )
}
export default ResultsPage;